{%- match kind -%}
{%- when EditedKind::Edited { rows } -%}
{% for row in rows %}{{ row.timestamp_prefix }}{{ indent }}{{ row.text }}
{% endfor -%}
{%- when EditedKind::UnsentWithDiff { who, diff } -%}{{ who }} unsent this message part {{ diff }} after sending!
{%- when EditedKind::Unsent { who } -%}{{ who }} unsent this message part!
{%- endmatch -%}